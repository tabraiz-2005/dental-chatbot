rm Procfile
printf 'web: uvicorn main:app --host 0.0.0.0 --port $PORT\n' > Procfile
cat Procfile
```

The `cat Procfile` will show you exactly what's inside. It should show:
```
web: uvicorn main:app --host 0.0.0.0 --port $PORT